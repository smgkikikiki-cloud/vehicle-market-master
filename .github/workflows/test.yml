name: Test vehicle market

on:
  push:
    branches:
      - main
      - feature/**
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt pytest

      - name: Compile Python
        run: python -m compileall -q vehreg pages tests

      - name: Validate backfill files and totals
        run: pytest -q tests/test_backfill_2026.py::test_backfill_gzip_months_have_expected_core_totals

      - name: Validate source selection
        run: pytest -q tests/test_backfill_2026.py::test_national_source_selection_prefers_future_official_csv

      - name: Run tests
        run: pytest -q
